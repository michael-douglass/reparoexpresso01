# Local placeholders so the app boots before real credentials land.
# Real values are delivered via /run/base44/app.env (platform secrets) and override these.

# Base44 — backend legado (opcional)
VITE_BASE44_APP_ID=
VITE_BASE44_APP_BASE_URL=

# Supabase — preenchido via secrets do painel
VITE_SUPABASE_URL=
VITE_SUPABASE_PROJECT_ID=
VITE_SUPABASE_PUBLISHABLE_KEY=
